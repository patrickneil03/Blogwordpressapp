# Ignore EVERYTHING by default
*

# Only include the Dockerfile
!Dockerfile

# Allow the config folder contents
!config/
!config/**

# Allow the entrypoint script
!entrypoint.sh

# 🎯 FIX: Explicitly allow the health check page through the filter
!health.php