# Ignore EVERYTHING by default
*

# Only include the Dockerfile
!Dockerfile

# Allow the config folder contents
!config/
!config/**

# 🎯 FIX: Explicitly allow the entrypoint script through the filter
!entrypoint.sh