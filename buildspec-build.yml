version: 0.2

phases:
  pre_build:
    commands:
      - echo "=== DEBUG: Checking Dockerfile ==="
      - echo "Line 18 content:"
      - sed -n '18p' Dockerfile
      - echo "Full Dockerfile head:"
      - head -25 Dockerfile
  build:
    commands:
      - echo "Building Docker image..."
      - docker build -t wordpress-blog-ecr:latest .
      - echo "✅ Docker image built successfully"